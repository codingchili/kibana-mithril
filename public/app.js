require('ui/autoload/all');

document.title = 'Authentication - Kibana';

var app = require('ui/modules').get('apps/kbn-authentication-plugin', []);

app.controller('kbn-authentication-plugin', function () {

});

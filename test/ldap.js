/**
 * @author Robin Duda
 *
 * Tests the connection to the LDAP server.
 */


const Assert = require('assert');

describe('LDAP Implementation', function () {

  before(function() {
   // todo set up the LDAP server mock.
  });

  it('Should bind successfully with a client.', function () {
    // todo just copy the response from wireshark
  });

  it('Should fail to bind with a client with wrong password.', function () {
    // todo just copy the response from wireshark
  });

  it('Should return a query response on username find.', function () {
    // todo just copy the response from wireshark
  });

});
